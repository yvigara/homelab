apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: nack-tls
spec:
  secretName: nack-tls
  duration: 2160h # 90 days
  renewBefore: 240h # 10 days
  issuerRef:
    name: ca-issuer
    kind: ClusterIssuer
  commonName: nack
